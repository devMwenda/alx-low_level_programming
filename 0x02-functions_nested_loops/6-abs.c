#include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @int: number to compute
 *Return: Always 0
*/
int _abs(int)
{
	if (int < 0)
	{
		return (-int);

	}
	else
	{
		return (int);
	}
	return (0);
}
