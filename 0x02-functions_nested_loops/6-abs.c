#include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @r: number to compute
 *Return: Always 0
*/
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);

	}
	else
	{
		return (r);
	}
	return (0);
}
