#include <stdio.h>
/**
 * main - entry point
 * printf - print string
 * Return: 0 if successful
*/
int main(void)
{
	char myStr[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", myStr);

	return (0);
}

