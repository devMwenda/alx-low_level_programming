#include <stdio.h>
/**
 * main - entry point
 * puts - prints string
 * Return: 0 if successful
*/
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
