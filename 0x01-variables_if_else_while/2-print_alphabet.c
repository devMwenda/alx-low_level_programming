#include <stdio.h>
/**
 * main - entry point
 * Return: 0 if executed successfully
*/
int main(void)
{
	char ch;

	ch = 'a';

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n')
	return (0);
}
